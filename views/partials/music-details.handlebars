<section class="card">
  <a href="/music/{{id}}">
    <header>{{title}}</header>

    <div class="card-body">
      <img src="/images/{{filename}}" <!--alt="{{description}}">-->
      <p>By: {{artist}}</p>
    </div>
  </a>
</section>